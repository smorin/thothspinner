# Milestone M14: Publishing to PyPI (v1.0.0)

## Goal/Requirement
Publish package to PyPI

**Sub-Requirements:**
- Finalize package metadata
- Set up GitHub Actions for CI/CD
- Publish to PyPI
- Create release notes and changelog

**Out of Scope:**
- New feature development
- Documentation updates (covered in M06 and M13)
- Component implementations

## Tests & Tasks

### Preparation Tasks
- [x] [M14-T01] Update pyproject.toml with complete metadata
      `pyproject.toml` has name, version, description, authors, readme, license, requires-python, and all project URLs.
- [x] [M14-T02] Add package classifiers and keywords
      11 PyPI classifiers and 10 keywords added to `pyproject.toml`.
- [x] [M14-T03] Set up proper versioning strategy
      Semver versioning with `uv_build` backend configured in `pyproject.toml`.
- [x] [M14-T04] Create LICENSE file (if not exists)
      `LICENSE` file exists (MIT, 2024 Steve Morin).
- [x] [M14-T05] Update main README with badges and installation instructions
      `README.md` includes `pip install thothspinner` and `uv add thothspinner` instructions.
- [x] [M14-T06] Create CHANGELOG.md with version history
      `CHANGELOG.md` exists with full history from v0.1.0 through v1.0.0.
- [x] [M14-T07] Add CONTRIBUTING.md guidelines
      `CONTRIBUTING.md` created with setup, dev workflow, code style, testing, commit conventions, and PR process.

### CI/CD Tasks
- [x] [M14-T08] Create GitHub Actions workflow for testing
      `.github/workflows/ci.yml` — test matrix on Python 3.11, 3.12, 3.13.
- [x] [M14-T09] Add workflow for linting and type checking
      `.github/workflows/ci.yml` — lint (ruff) and typecheck (ty) jobs included.
- [x] [M14-T11] Set up automated release workflow
      `.github/workflows/publish.yml` — triggers on `v*` tags; builds, publishes to TestPyPI, then PyPI.
- [ ] [M14-T12] Configure OIDC Trusted Publishing environments in GitHub repository settings
      The `publish.yml` workflow uses `--trusted-publishing always` (OIDC), not API tokens.
      Requires creating `pypi` and `testpypi` environments in GitHub repo settings and registering
      the repository as a trusted publisher on PyPI.org and test.pypi.org.
- [x] [M14-T13] Add workflow for publishing to TestPyPI first
      `publish.yml` includes a `publish-testpypi` job that runs before `publish-pypi`.

### Publishing Tasks
- [x] [M14-T14] Build distribution packages (wheel and sdist)
      `dist/thothspinner-1.0.0-py3-none-any.whl` and `dist/thothspinner-1.0.0.tar.gz` exist locally.
      Use `just build` or `uv build` to rebuild after any changes.
- [ ] [M14-T15] Test installation from TestPyPI
      Run `just release 1.0.0` to push tag and trigger the workflow; verify TestPyPI install succeeds.
- [ ] [M14-T16] Publish to production PyPI
      Automated via `publish.yml` after TestPyPI step passes.
- [ ] [M14-T17] Create GitHub release with tag v1.0.0
      Run `just release 1.0.0` — creates tag, pushes to origin, triggers CI publish workflow.
- [ ] [M14-T18] Announce release (GitHub discussions, etc.)

### Verification Tasks
- [x] [M14-TS01] Verify package builds correctly
      `uv build` produces valid wheel + sdist; artifacts present in `dist/`.
- [ ] [M14-TS02] Test installation from PyPI in clean environment
      `pip install thothspinner` in a fresh venv; verify imports and basic usage.
- [x] [M14-TS03] Verify all dependencies are properly specified
      `rich>=13` and `textual>=6.1.0` declared in `pyproject.toml`; no missing runtime deps.
- [ ] [M14-TS04] Check that examples work with PyPI version
      Run key examples against the installed PyPI version (not local src).
- [ ] [M14-TS05] Validate GitHub Actions workflows run successfully
      Verify CI (test/lint/typecheck) and publish workflows all pass on the `v1.0.0` tag push.
- [ ] [M14-TS06] Ensure documentation is accessible
      Verify all GitHub URLs in `pyproject.toml` (Homepage, Docs, Changelog, Bug Tracker) resolve correctly.

## Deliverable
```bash
# Users can install from PyPI
pip install thothspinner

# Or with UV
uv add thothspinner
```

## Automated Verification
- GitHub Actions: All CI/CD workflows pass
- `uv build` - Package builds successfully
- `twine check dist/*` - Package passes PyPI checks

## Manual Verification
- Package installs correctly from PyPI
- All components work as expected
- Documentation links work
- GitHub release page is complete
- Version tags are properly created
